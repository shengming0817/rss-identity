# #2435 PR #1028 review 修复与复验

关联：[PR #1028](https://dev.azure.com/shengming0923/rss/_git/rss-identity/pullrequest/1028)。本轮生产/消费者源码固定 A：`27fc0c593f239c6186194d5843da412e3349c171`。本证据提交 B 仅改变 docs；生产/config/消费者源码 SHA-256 与 A 相同。之前的 [ship 记录](202609170600-2435-embedded-authentication.md) 保留其历史 revision 与结论，不用旧证据替代本轮。

本轮只处理绑定 `8df64b3803179c383c5808bb1adf210f2ffd6959` 的最新 review，F1–F9 均 CONFIRMED、IN_SCOPE；没有重开上一轮 ship 的七项 findings。范围依据是 #2435 的嵌入组件、私有 HTTP DTO、宿主管理策略和最小参考宿主验收。对比 develop 与本分支历史，九项均来自当前重构后的活动路径；未发现应保留的兼容惯例。

计划复核：保持可选认证事实，统一全部活动 HTTP/config JSON，补齐管理策略覆盖，复用现有 schema/probe，恢复 app seam，闭合公开诊断分类。未添加兼容别名、双路径、通用 contract/codegen 或迁移框架。现在修复：都属于原验收范围，依赖已齐备，不改 RSS 底座 trait，不改变现有 schema/事件格式或事务结算语义，无 defer/OOS。

F6/F9 的单次批量请求为 `Q-9c0f01427eda4fa7a9c36905cb081391`，建议均在当前 PR 修复。120 秒后工具返回 expired、answers=[]；按用户本次 AGENTS 的“两分钟无响应，按推荐选项继续”授权执行，未把超时记作主动回答。

| Finding | 原定位 | 根因与实际修复 | 验证 |
|---|---|---|---|
| F1 P1/Cx2 | crates/identity-http-axum/src/dto.rs:50 | 可选 upstream auth_time 被投影层 expect 强制解包；保持 Option，JSON 为 authTime:null，不伪造时间。 | DTO 先 panic RED 后 GREEN；真实 PG 联邦会话 GET session/security 返回 200、no-store 和 null。 |
| F2 P1/Cx2 | crates/identity-postgres/src/operations.rs:288 | proof/authorize 的 if/else 使密码证明替代宿主权限；增加 ChangeOwnPassword，在同一写事务重查 proof 并无条件调用 policy。 | 撤销权限后旧代码仍成功，RED；修复后拒绝且不写事件；RecentMfa 拒绝；显式允许成功、旧会话撤销。 |
| F3 P1/Cx2 | app/identity/src/migration.rs:52 | fresh 安装与启动 probe 脱节；新增 verify_profile 复用同一 probe，并以 SET LOCAL ROLE 分别验证实际 runtime/maintenance 后提交。额外拒绝 RSS relation 上的 PUBLIC ambient grants。 | BYPASSRLS 旧实现提交，RED；六组危险属性/继承/默认 ACL/授权选项/PUBLIC 均拒绝且两个 schema 不存在；修复输入后可安装。 |
| F4 P1/Cx2 | Makefile:9 | 重构移除了 reference app 的真实 provider 接缝，库测试不能证明安装和资源回收；新增当前最小宿主 installation suite、TLS PG runner、test-assembly 并纳入 ci。 | 正常与重复安装、失败回滚、多租户管理、普通账户改密、bind 失败/正常 stop、PG 连接归零、关闭后 KDF 拒绝，以及真实 HTTP 登录。 |
| F5 P1/Cx2 | app/identity/src/assembly.rs:48 | 多租户 admission 配置与单 bootstrap policy 集合不一致；bootstrapAccounts 与 tenants 精确一对一覆盖，策略按 AccountKey 匹配。 | 两租户一管理员旧实现接受，RED；缺失/重复/外部 tenant 拒绝；两租户初始化、登录、各自管理及防自锁均通过。 |
| F6 P1/Cx3 | crates/identity-http-axum/src/dto.rs:3 | 私有 DTO 遗漏统一 Serde 命名，测试也固化旧形状；全部应用请求/响应 DTO 使用 camelCase、请求 deny_unknown_fields；参考配置同步，外部 OIDC 标准字段仍按规范。 | 先修改 wire golden 得到 RED；精确 JSON、旧字段拒绝、真实 HTTP/PG/Keycloak 调用方通过。独立消费者也验证 camelCase 登录输出。 |
| F7 P2/Cx1 | app/identity/src/bin/admin.rs:70 | 纯配置构造位于 runtime 获取后的清理范围外；把 AuthorityConfig、PG 输入与 binding 构造全部前移，获取资源后结果统一经过有界清理。 | 逐条审计早退路径；app/CLI、文件配置和真实 maintenance seam 回归通过。没有声称获得旧路径资源泄漏的运行时复现。 |
| F8 P2/Cx2 | crates/identity-http-axum/src/lib.rs:23 | ConnectInfo 与 ClientAddress 是不同扩展，公开文档遗漏转换；rustdoc/指南明确 host middleware 必须注入，并给出仅信 accepted peer 的最小实现。 | 固定旧 Git 消费者仅加 ConnectInfo，登录 503，RED；新独立消费者验证缺扩展 503、正确 middleware 后 200/cookie/camelCase，同时携带伪造 forwarded header。 |
| F9 P2/Cx3 | crates/identity-postgres/src/types.rs:43 | 单一 Invalid 横跨配置、输入与截止期导致错误 HTTP 400；删除该变体，拆成 Configuration、InvalidInput、DeadlineElapsed，保留认证、存储与 settlement 分类。 | 旧 keyring Display 断言 RED；新分类/HTTP 400 对 503/内部 HttpFailure 通过，原 settlement 和真实 PG deadline 回归通过。 |

<details><summary>逐项方案、取舍、影响文件与参考</summary>

- F1：A 统一拒绝缺时间的 OIDC（约 10 行，少但错误缩窄合法协议）；B 保持 optional 并覆盖真实 HTTP（约 40 行，选择）。影响 dto.rs、management_http.rs、wire v2；依据 OIDC Core §2，普通 ID token 的 auth_time 允许缺失。
- F2：A 仅对现有 ResetPassword 分支追加策略（约 5 行，无法区分本人改密）；B 增加明确 operation 与同事务授权（约 80 行含测试，选择）。影响 operations.rs、management.rs、embedded.rs、参考 policy 与 HTTP fixture；参考 Ory Kratos privileged-session/AAL 设置入口，具体授权仍归 RSS 宿主。
- F3：A 安装前仅检查角色属性（约 20 行，遗漏继承/有效 ACL）；B 在提交前按目标 current_user 调用同一 probe（约 40 行加共享安装测试，选择）。影响 schema.rs、lib.rs、migration.rs、installation.rs；依据 PostgreSQL SET ROLE 与 SQLx 事务回滚。未复制第二份 Identity privilege manifest。
- F4：A 只恢复安装成功测试（约 70 行，遗漏失败资源路径）；B 当前 minimal host 的完整 T2 seam（约 300 行测试及约 20 行 runner，选择）。影响 Makefile、providers.py、installation.rs；复用现有 provider lifecycle，按 SQLx/RSS ownership 清理。测试不声称等价部署/T3。
- F5：A 只接受单租户（约 10 行，简单但收缩既有声明）；B 唯一、完整的 tenant→AccountKey 配置（约 50 行加共享测试，选择）。影响 config.rs、assembly.rs、example.json、files.rs、installation.rs 和嵌入文档。该项为业务覆盖缺陷；不引入外部 RBAC 模型。
- F6：A 只加 rename_all 并替换现有断言（约 100 行，容易再漂移）；B 私有 DTO + 精确 wire golden/旧字段拒绝/真实 consumer（约 160 行并同步配置文档，选择）；C contract 生成 DTO/文档（新公共包及生成链，复杂度和耦合更高）。依据 Serde rename_all/deny_unknown_fields；不保留 snake_case alias，不改变持久化和事件 owner 的 JSON。
- F7：直接前移纯验证（约 30 行移动），不添加资源 guard 抽象。影响 admin.rs；配置失败不会先取得 pool/KDF。执行和 authority 构造结果仍统一清理。
- F8：A 只修 rustdoc（约 5 行，不能证明宿主可登录）；B rustdoc + 嵌入 middleware + 独立实际请求（约 70 行，选择）。影响 lib.rs、embedding.md、tests/consumers/local/lib.rs；依据 axum 0.8.9 ConnectInfo 源码。local consumer 仍不引入 reqwest/openidconnect/rsa。
- F9：A 只拆 Configuration/InvalidInput（约 30 行，截止期仍误归输入或不可用）；B 三个封闭分类并更新所有产生点/HTTP 映射（约 70 行，选择）；C 独立错误 contract/映射生成器（新框架，当前不需要）。影响 postgres 的 lib/runtime/credentials/operations/sessions/types 与 HTTP boundary/management、文档；参考固定 RSS MessagingErrorKind + settlement 分层。密码 KDF 的事务前超时使用 DeadlineElapsed，已进入事务仍由 RSS owner 返回结算事实。

参考原文：[OIDC Core §2](https://openid.net/specs/openid-connect-core-1_0-18.html#IDToken)、[Serde container attributes](https://serde.rs/container-attrs.html)、[PostgreSQL 17 SET ROLE](https://www.postgresql.org/docs/17/sql-set-role.html)、[Ory Kratos v1.3.1 settings](https://github.com/ory/kratos/blob/v1.3.1/selfservice/flow/settings/handler.go)。同时实际读取 axum 0.8.9 `extract/connect_info.rs`、SQLx 0.9.0 `transaction.rs::Drop` 和固定 RSS `93ce684` 的 `MessagingErrorKind` / managed listener 源码。

</details>


## 复验结果与证据边界

| 实际执行 | 结果 |
|---|---|
| cargo test --locked：受影响 app/postgres/http 的 lib/bin/files | 全部通过；provider 用例另由强制 runner 执行。 |
| make check | fmt、workspace all-targets/all-features clippy -D warnings、普通 lib/bin build 全部通过。 |
| Python unittest discover | 34 passed。 |
| make test-pg | 54 个 canonical PostgreSQL 17.6 场景 passed，无跳过。 |
| make test-federated | 6 个真实 Keycloak TLS 联邦 HTTP 场景 passed。 |
| make test-assembly | 1 个完整 TLS PostgreSQL reference host 场景 passed，内部覆盖六组拒绝/回滚及正常启动/停止。 |
| 固定 A 的两个独立消费者 | 各自全新目录/target，Git 完整 SHA、独立 lock，fmt/clippy/deny/test 均通过；各 1 passed/0 failed/0 ignored。 |
| 冲突预检 | fetch origin 后 PR MERGEABLE。 |

[消费者 report.json](202609170715-2435-fix-consumers/report.json) 保存完整 package ID 闭包、Git revision、provider digest、文件 hash 与实际结果。锁文件分别为 [local](202609170715-2435-fix-consumers/local/Cargo.lock)、[OIDC](202609170715-2435-fix-consumers/oidc/Cargo.lock)；同目录保存各自 manifest 和 deny 配置。local 闭包排除 openidconnect/reqwest/rsa；OIDC 既有公开验签依赖例外仍由 #2357 跟踪，未扩张。

复现时 checkout A，并选择仓库/Cargo 配置祖先之外的不存在目录：

```sh
python3 hack/check_consumer.py --revision 27fc0c593f239c6186194d5843da412e3349c171 --output /tmp/identity-1028-fresh-proof
```

最终 `make ci CI_BASE=origin/develop` 按 fix 流程在 pm:fix 与 needs-check-fix label 之后执行，结果追加到 PR；本文件不提前宣称该步骤已执行。当前证明为组件 T1/T2 和最小参考宿主 seam，不冒充 #2436 部署、UI、候选 artifact 或独立产品 T3。全部九项已修，无遗留/defer/OOS；F6/F9 按已说明的单次批量处置结果执行。下一步由独立 check 复核，fix 不直接标 ready。
