# #2341 / PR #1004 五组修复与复验

用户明确指定五组均在当前 PR 修复，继续归属 PBI #2341。七条 finding 均 CONFIRMED / IN_SCOPE：直接来自本 PR 的 T3 控制器、证据漏斗、模拟产品消费者和执行入口。不存在 deferred 或新 PBI。

| 分组 | Findings / 原评级 | 根因与修复 |
|---|---|---|
| 固定构建输入 | F1 P1/Cx1 | `Dockerfile:2` 的 live apt/npm 没有可信预期。固定 Ubuntu `20260912T000000Z` 签名快照及 python3/openssl/libnss3-tools 精确版本；pnpm 11.4.0 tarball 在执行前验证 SHA-512，依赖继续按 lock integrity 安装。无需镜像发布平台。 |
| 封闭证据字段 | F2 P1/Cx2 | `evidence.py:111` 先前验证局部却原样返回完整 candidate。新增候选 v1 闭合字段/类型/值检查，递归构造已验证对象；toolchain 原文不公开，顶层及每层未知字段拒绝。 |
| 统一会话判定 | F3 P1/Cx2、F7 P2/Cx2 | `consumer.mjs:39,149` 统一 `/app` 与 API 的在线验证。401/403 清会话，429/503 保留并拒绝当前访问，返回 Retry-After；页面提供重新登录或重试。故障恢复用同一浏览器页面/API 检查 cookie、handle、session ID，消除私有探针绕过产品路由的证明缺口。 |
| 补齐静态门禁 | F4 P2/Cx1、F5 P2/Cx1 | `Makefile:69` 解析全部载体 Python/JS 及 bounded_process helper；锁定 ESLint 并执行 no-undef（含 typeof），覆盖所有 mjs。`scenario.mjs:20` 用显式词法 member。 |
| 原子占用子网 | F6 P2/Cx2 | `run.py:79,107` 用 Docker network create 取得两网络所有权，部分失败回滚，真实重叠才换下一对；外部引用前校验候选与实际网络配置。保留 `/24` 子网、上半段 `/25` 动态范围、固定地址及 internal 隔离；服务地址和网络成员不改写。 |

架构影响局限于测试载体，无 Rust trait、migration 或产品装配语义变更。历史 HEAD `a8ae598` 的成功记录有私有 probe 代替浏览器恢复的缺口；本轮保留历史记录并补充新证据。合入最新 develop 后保留既有生命周期入口及部署修复。

Cx2 方案比较与处置：F2 的最小方案仅输出 allowlist，采用闭合 schema + 安全投影以拒绝未知输入；F3/F7 的最小方案仅收窄清除条件，采用公共 guard 并验证页面/API；F4/F5 的最小方案手列两文件，采用全目录解析/no-undef；F6 的最小方案 create 后直接 external，采用占有/回滚并在转换前核对完整受支持 IPAM。均采用根因方案，当前完成，无兼容双路径、TODO 或延期窗口。

执行清单：五组修复、最小正反向验证、固定候选 T3、完整 make ci、commit/push、PR 留痕与复查交接。所有工作继续由 #2341 跟踪。

验证：

- `make check-t3-local-auth`：15 Python 测试、9 JS 测试、全载体语法与 ESLint 通过；pnpm audit 无已知漏洞。
- 正反向：每一层候选对象注入 password 均拒绝；真实 runner 对嵌套 token 拒绝于 Docker 启动前，失败 receipt 不含敏感值或 candidate。显式注入未声明 member，no-undef 返回失败。
- 两个真实 Docker 进程同时申请网络，取得不相交对子网；单测覆盖第二网冲突回滚、不删除竞争者、候选 IPAM/internal 漂移拒绝、服务固定地址保持。
- 控制器镜像从固定 snapshot/包版本/pnpm 摘要构建成功。安装不运行未校验 npm 内容或依赖生命周期脚本。

固定候选：沿用 `06d4e85498281a874eff9898a3b77fc19705e8b1`，manifest SHA-256 `01429fb94beed6e361749c1342f8f93389f18de47862a82a861c73a407ddd568`。不重新构造或修补产品 artifact。

首轮固定载体 `6eb065c` 在 fixture_prepare 因 candidate_network_mismatch 拒绝，产品尚未启动，清理通过。原因是该候选已有 `.128/25` 动态分配范围，预留网络必须准确保留；修正后才继续完整场景。此失败记录用于证明拓扑漂移不会被 external 投影吞掉。

上游参考（未复制实现）：[Ubuntu Snapshot Service](https://snapshot.ubuntu.com/)、[Compose networks](https://docs.docker.com/reference/compose-file/networks/)、[oauth2-proxy stored_session.go](https://github.com/oauth2-proxy/oauth2-proxy/blob/master/pkg/middleware/stored_session.go)、[ESLint no-undef](https://eslint.org/docs/latest/rules/no-undef)。

边界：消费者是 Identity T3 fixture，不代替真实 MDM 资源授权接入。429 的会话保留由单元测试证明；真实 PG 故障使用 503，经过浏览器/API。长期会话、生产容量/SLO 等仍沿用原验收未覆盖项。

完整本地 CI：`make -k ci CI_BASE=origin/develop` 一次完整执行，退出码 0；覆盖 Makefile 全部 ci 目标，包括真实 provider 与恢复检查。验证代码 HEAD 为 `4ab6edcf052b1e8aba9f872762da52a8e2941e6c`，日志 SHA-256 `1be22dccbdab6cc6298067cf9b0f564cf24ab897ec0eafd32478caa58a681c69`。执行后段只有报告/receipt 文档待提交，故 recovery 输出 dirty=true；代码、配置和测试载体未再改动，无需重复整轮 CI。

最终固定候选 T3：**10/10 场景通过，资源清理通过**。UTC 2026-09-13T00:07:29Z 至 2026-09-13T00:21:00Z，载体 Git `4ab6edcf052b1e8aba9f872762da52a8e2941e6c`，控制器 `sha256:a7103dc57639e049356b92d20939b46a42fd2df39d8f20adf5341408bc32516c`。同一浏览器 storage_failure 为 503 → 200，same_browser_session=true，session_id=`5c18926a-7a97-4763-b616-182965d0c4b3`；撤销与匿名页面均拒绝访问。

- [完整通过回执](202609130021-2341-five-group-t3-passed.json)，SHA-256 `6cc55e1e259d9a528f00c6568c1f247ef0c9be9e1a92f8b75cc4864b9ff6528d`。
- [IPAM 前置拒绝回执](202609130006-2341-network-preflight-failed.json)（修复预留范围前；清理通过）。

本轮七条 finding 全部修复；只增加本 PBI 的记录，不新增 issue，不提前关闭尚待复查的 PR。
