> 历史候选验收记录；以下旧命令仅在记录指定的固定源码 checkout 中恢复，不适用于当前镜像入口。

# #2341 本地认证 T3：原则复核与实施记录

## 原则与最终方案

- 彻底：从固定候选的正式 renderer/operator、PG、Hydra、gateway/server 和实际 UI 开始；浏览器建立中央/产品会话，保留同一未过期凭据在线复核，并用另一有效身份排除全局不可用导致的假撤销。
- 不向后兼容：只接受当前完整 candidate v1、固定 linux/amd64 与修正后的标准 HTTPS 443 拓扑；不增加旧配置、旧端口或鉴权旁路。产品缺陷另开实现 PR，T3 不修补候选。
- 优雅简洁：可丢弃 Node wire consumer 只调用 openid-client 公共接口和 Identity validate；没有新 SDK、认证缓存或资源授权层。直接消费候选 gateway，删除临时 TCP 转发进程。

## 依赖顺序与 owner

所有文件由 #2341 主 agent 实施；reviewer 只读复核。

1. `evidence.py`、`test_evidence.py`：固定 candidate / carrier 来源，定义完整场景与安全事件证据拒绝条件。
2. `consumer.mjs`、`consumer.test.mjs`：Code/PKCE、state/nonce/签名和逐请求 validate；控制面仅通过私有 Unix socket 复核内存中的原凭据。
3. `prepare.py`、`run.py`、`test_run.py`、Dockerfile/lock：秘密私有卷、正式 UID/TLS、独占资源、冻结控制器构建输入、失败记录和清理。先于浏览器场景执行。
4. `scenario.mjs`：登录、保护、刷新、退出/全部退出、密码变化、禁用/恢复、PG/Hydra 故障和 Outbox 闭环。
5. Make/README 与本记录：最小回归 → 固定候选实跑 → 完整结果 → ship 评论/label → 产品本地 CI → 延迟 monitor。

TDD 已先观察证据与 consumer 用例失败后实施；补充的取消、清理错误、协议响应、超时及 receipt 自引用用例也先失败再修复。当前最小入口为 `make check-t3-local-auth`。

## 首审与处置

首审固定提交 `3aa5b81`，三名 reviewer 覆盖六维度，共 9 条 IN_SCOPE（P1 4/P2 5，Cx1 5/Cx2 4），在 `d4b359a` 修复：

| ID | 原定位 | 根因与修复 |
| --- | --- | --- |
| F1 P1/Cx1 | run.py:56 | 共享可变镜像 tag → 唯一 tag 构建后全部使用 image ID，并核对实际实例 |
| F2 P1/Cx1 | run.py:127 | Chromium CA 未覆盖 Node TLS → NODE_EXTRA_CA_CERTS 保持严格校验 |
| F3 P1/Cx2 | consumer.mjs:71 | 协议错误被合成为 503 → 分离传输/响应错误，场景要求真实 Identity 503 |
| F4 P1/Cx1 | scenario.mjs:216 | 启用后缺恢复登录 → fresh context 登录并保留旧 grant 拒绝 |
| F5 P2/Cx1 | run.py:59 | helper 未绑定 → 对应 Git blob 逐字节检查并计入 SHA |
| F6 P2/Cx1 | run.py:179 | SIGTERM 绕过 finally → 受控中断、清理、失败记录和 handler 恢复 |
| F7 P2/Cx2 | run.py:23 | 命令失败被压平 → stage/operation/退出码/固定诊断/日志摘要 |
| F8 P2/Cx2 | run.py:232 | 清理错误跳过余项或丢记录 → 每资源独立尝试并记录标识与错误 |
| F9 P2/Cx2 | run.py:51 | 前置失败无记录 → 统一异常边界、独占输出与原子落盘 |

复核新发现 2 条 P2/Cx1，在 `79aff10` 修复：F10 `run.py:40` 超时缺操作诊断，统一转换为 CommandFailed；F11 `run.py:144` 输出位于载体目录时自引用，改为 Git 固定文件清单并从同一份固定字节构建。三名 reviewer 已分别确认原 findings 与新增项修复，无 defer/OOS 项。

## 固定候选实跑

旧候选 Identity `7e1e11dfcfad18af764f60a18671be536640573d`，UI `37b7fb356aa7e436cc708caa1593427e6d553d30`，manifest SHA-256 `f4549eebeb00f216c23e7df83078ba66609c88e5a20fd2fab8e75524b27c2888`。

[旧候选失败记录](202609120745-2341-local-auth-t3-failed.json)（run-05，开发诊断快照，尚未冻结载体提交）在 `volume_initialize` 失败，诊断 `invalid_interpolation_format`，清理通过。候选 Compose 未转义 shell 美元符号，正式安装尚未发生，因此没有浏览器/撤销通过结论。同一缺陷由 [#2419](https://dev.azure.com/shengming0923/rss/_workitems/edit/2419) 独立修复；重复项 #2420 已关闭并指向 #2419。

新固定候选 Identity `06d4e85498281a874eff9898a3b77fc19705e8b1`，UI SHA 不变，manifest SHA-256 `01429fb94beed6e361749c1342f8f93389f18de47862a82a861c73a407ddd568`。

- run-06：安装/健康/管理员浏览器登录完成；载体事件版本期待错误导致拒绝。按 RSS ContractVersion 的 `v{N}` 规范修正并增加回归（`5f6c7ff`）。
- run-07：八个场景通过，所有撤销结果均为在线 403，旧凭据剩余 295–298 秒，有效对照 200。Hydra fault 排在其他撤销之后，worker 遇到前序失败积压，目标 grant 未在固定 45 秒内转 revoking。已将独立 fault 提前，在正常场景继续前取得在线撤销证据，末尾统一等待 durable cleaned（`7e6d5c9`）。同次退出暴露一次性容器 auto-remove 与清理枚举竞态；现在再次确认不存在时视作成功，其他错误继续保留。

run-08 已完成所有即时场景后，由主 agent 主动取消以补强 worker 失败观测；中断记录和清理通过。run-09 在目标 grant 的 `revoking` 与正式 worker 固定失败计数同时成立后才恢复 Hydra，末尾继续等待该 grant 的 `cleaned`，原始日志不进入 RPC/记录。

[最终固定候选记录](202609120803-2341-local-auth-t3-passed.json)（run-09）**通过**。执行时间 2026-09-12 07:49:32–08:03:22 UTC，冻结载体提交 `7bec0dea26bc2d14aa6c429e9c11ba628fb926bb`，Chromium `148.0.7778.0`。记录 SHA-256 `383eaf8a7a3f2ed4b2cdd9d5d5062e2b66497f8cd0cdd7cc17f470d01b785fdf`；记录所列全部载体文件在 Git 提交与当前工作树中逐项摘要一致。

| 场景 | 结果 |
| --- | --- |
| 登录/产品交接、保护性拒绝、刷新 | 通过 |
| 当前退出 | 旧凭据在线 403，剩余 242 秒，对照 200 |
| 全部退出 | 两个独立会话均拒绝；记录凭据剩余 298 秒，对照 200 |
| 密码变化 | 旧凭据在线 403，剩余 298 秒；旧密码拒绝，新密码登录成功 |
| 禁用/重新启用 | 旧凭据在线 403，剩余 297 秒；启用后 fresh context 可登录，旧 grant 未复活 |
| PG 故障与恢复 | private validate 与本地登录真实 503；恢复后原有效凭据 200 |
| Hydra 清理故障与恢复 | 正式 worker 失败计数增加 1；恢复后旧凭据在线 401、剩余 245 秒、对照 200；同一 grant 最终 cleaned |
| 安全事件 | 76 条通过版本/schema/字段与动作关联检查；终点为 durable Outbox |
| 清理 | passed，cleanup_failures 为空 |

载体最小回归为 12 个 Python 用例、3 个 Node 用例，独立依赖公告检查通过。最终产品 `make ci CI_BASE=origin/develop` 按 ship 流程在交接 label 后执行，实际结果由 PR 的本地验证评论持有。

恢复此证据：从 Identity forge 获取固定 `06d4e854...` 的完整源码对象，按 [候选构建说明](https://dev.azure.com/shengming0923/rss/_git/rss-identity?path=/docs/deployment/candidate.md&version=GC06d4e85498281a874eff9898a3b77fc19705e8b1) 使用固定 rss-web `37b7fb356...` 构建并核对 manifest/OCI 摘要；原验收消费的是构建产生的具体 archives，而非重新构建必然逐位相同的承诺。测试源码可由上述载体 Git 提交恢复，执行命令为 `make test-t3-local-auth IDENTITY_T3_CANDIDATE=/absolute/candidate IDENTITY_T3_RECORD=/absolute/new-record.json`。

## 边界

事件终点为持久 Outbox，不宣称 broker delivery；模拟消费者不代表 MDM/ABAC 集成。不覆盖联合身份、MFA/恢复、生命周期全矩阵、长期时间边界或生产容量/SLO。秘密、cookie/token、密码、原始浏览器 trace 均不进入公开记录。
